// Copy this file to `firebase.config.js` and replace the placeholder values with your Firebase project's values.
// Do NOT commit `firebase.config.js` to version control — keep it local and secret.

export default {
  apiKey: "YOUR_REAL_API_KEY",
  authDomain: "your-project-id.firebaseapp.com",
  projectId: "your-project-id",
  storageBucket: "your-project-id.appspot.com",
  messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
  appId: "YOUR_APP_ID"
};
